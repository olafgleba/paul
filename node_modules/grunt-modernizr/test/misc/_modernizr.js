(function () {
	function doSomething() {}

	var m = window.Modernizr;

	if (m.fontface) {
		doSomething();
	}

	if (!m.svg) {
		doSomething();
	}

	if (m.contenteditable && !m.csstransforms3d) {
		doSomething();
	}

	if (m.overflowscrolling&&m.contextmenu) {
		doSomething();
	}

	if (m.scriptdefer===true) {
		doSomething();
	}

	if (m.fontface !== true) {
		doSomething();
	}

	if (m.prefixed("transform")) {
		doSomething();
	}

	if (m.mq) {
		doSomething();
	}

	if (m.applicationcache) {
		doSomething();
	}

	if (m.audio) {
		doSomething();
	}

	if (m.canvas) {
		doSomething();
	}

	if (m.geolocation) {
		doSomething();
	}

	if (m.history) {
		doSomething();
	}

	if (m.hsla) {
		doSomething();
	}

	if (m.indexeddb) {
		doSomething();
	}

	if (m.input) {
		doSomething();
	}

	if (m.inputtypes) {
		doSomething();
	}

	if (m.localstorage) {
		doSomething();
	}

	if (m.multiplebgs) {
		doSomething();
	}

	if (m.postmessage) {
		doSomething();
	}

	if (m.sessionstorage) {
		doSomething();
	}

	if (m.textshadow) {
		doSomething();
	}

	if (m.rgba) {
		doSomething();
	}

	if (m.video) {
		doSomething();
	}

	if (m.webgl) {
		doSomething();
	}

	if (m.websqldatabase) {
		doSomething();
	}

	if (m.webworkers) {
		doSomething();
	}
}());
